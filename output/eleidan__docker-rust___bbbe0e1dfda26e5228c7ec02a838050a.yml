version: '2'
services:
  dev:
    image: eleidan/rust:nightly-jessie
    environment:
      - CARGO_HOME=/home/phantom/app/.cargo
    volumes:
      - .:/home/phantom/app
      - .docker/.bash_history:/home/phantom/.bash_history

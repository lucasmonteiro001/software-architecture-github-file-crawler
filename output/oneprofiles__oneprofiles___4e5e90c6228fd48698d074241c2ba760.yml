server:
  image: server
  hostname: op-server
  volumes:
    - /opt/op/server
  ports:
    - "8080:8080"

server:
  image: front
  hostname: op-front
  volumes:
    - /opt/op/front
  ports:
    - "3000:3000"

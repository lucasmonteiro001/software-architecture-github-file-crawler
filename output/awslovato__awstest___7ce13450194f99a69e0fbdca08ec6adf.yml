# Don't forget this is yaml.
# Use this to stage your own compose file as an example/template.  Check hub.docker.com for the container you
# wish to use and make sure you have the format and variables correct.

#postgres:
#  image: postgres:9.4.5
#  environment:
#    POSTGRES_USER: $USER 
#    POSTGRES_PASSWORD: $PASS 
#  ports:
#    - '5432:5432'
#  volumes:
#    - ~/.docker-volumes/$APP/postgresql/data:/var/lib/postgresql/data

#redis:
#  image: redis:2.8.22
#  ports:
#    - '6379:6379'
#  volumes:
#    - ~/.docker-volumes/$APP/redis/data:/var/lib/redis/data

#app:
#  build: .
#  command: $CMD  # what your app will do when container starts
#  environment:
#    PYTHONUNBUFFERED: "true"
#  links:
#    - postgres
#    - redis
#  volumes:
#    - .:/$DIR		#Same as Dockerfile. This is your data.
#  ports:
#    - '8000:8000'

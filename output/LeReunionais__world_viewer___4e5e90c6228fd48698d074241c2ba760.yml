viewer:
  build: .
  ports:
    - "9000:9000"
  environment:
    - REGISTRY_HOST
  restart: always
test:
  build: .
  command: echo "No tests defined"
coverage:
  build: .
  command: echo "No coverage defined"

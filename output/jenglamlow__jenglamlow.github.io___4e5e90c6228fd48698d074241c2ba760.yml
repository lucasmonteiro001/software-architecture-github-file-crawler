jekyll:
    image: starefossen/github-pages
    command: jekyll serve -d /_site --force_polling --drafts --watch --incremental -H 0.0.0.0
    ports:
        - 4000:4000
    volumes:
        - .:/usr/src/app

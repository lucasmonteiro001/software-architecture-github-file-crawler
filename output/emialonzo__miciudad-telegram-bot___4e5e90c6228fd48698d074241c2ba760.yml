  version: '2'
  services:
    web:
      image: node:6.9.1
      ports:
       - "5000:5000"
      volumes:
       - .:/code


    # version: '2'
    # services:
    #   web:
    #     build: .
    #     ports:
    #      - "5000:5000"
    #     volumes:
    #      - .:/code
    #     depends_on:
    #      - redis
    #   redis:
    #     image: redis

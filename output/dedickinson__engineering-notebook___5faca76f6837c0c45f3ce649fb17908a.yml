jenkins:
    container_name: jenkins
    restart: always
    image: dedickinson/jenkins-local
    ports:
        - 8080:8080
        - 8000:5000
    volumes:
        - ~/var/jenkins:/var/jenkins_home

#version: '2'

# CircleCI.com uses docker version 1.9.1 - so use a Version 1 docker-compose.yml file for now.

#services:
    todocore:
        image: klmcwhirter/todocore
        ports:
            - "5000:5000"

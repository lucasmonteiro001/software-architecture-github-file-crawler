web:
  container_name: spa                      # Name of container used by docker
  image: docker.sebastian-software.de/spa  # Name of docker image used
  ports:
  - "80"                                   # Publish port 80 to consul
  restart: always                          # Don't forget :)
